# Мой Сайт — Внешняя память проекта

## Технологический стек
- **Backend**: Python (Flask)
- **Frontend**: HTML5, Vanilla CSS, Vanilla JavaScript
- **Стилизация**: Чистый CSS без фреймворков

## Структура проекта
- `/templates/` — HTML-шаблоны Jinja2
  - `base.html` — базовый скелет всех страниц (`<!DOCTYPE>`, `<head>`, шапка, `<script>`). Блоки: `title`, `head`, `main_class`, `content`, `body_extra`
  - `macros.html` — коллекция переиспользуемых макросов (макрос `retro_button` — многослойная ретро-кнопка в одном вызове)
  - `_header.html` — partial шапки сайта (логотип + 2 nav-кнопки через `retro_button`), активность берёт из контекста `active`
  - `_sidebar_menu.html` — partial бокового меню «Содержание» (5 пунктов генерируются циклом `{% for %}`)
  - `index.html` — Главная (`{% extends 'base.html' %}`, окно «В разработке»)
  - `resume.html` — Резюме (`{% extends 'base.html' %}`, 5 секций + include сайдбара)
- `/static/`
  - `/css/` — файлы стилей (например, `style.css`)
  - `/js/` — скрипты (например, `main.js`)
  - `/images/` — логотип (`logo.svg`) и `/images/icons/` — ретро-иконки
- `/References/` — макеты и референсы (кнопки, шапка, контент; устаревшие версии, не источник истины)
- `app.py` — главный файл Flask-приложения
- `requirements.txt` — зависимости проекта
- `AGENTS.md` — этот файл (внешняя память)

## Команды запуска и разработки
- Виртуальное окружение: `.venv`
- Запуск приложения: `.venv/bin/python app.py` (запущено на порту 5000 в режиме отладки `debug=True`)

## Принятые технические решения
1. **Анализ референсов кнопок (Normal)**:
   - Исходный цвет кнопки: `#ebebeb` (серый).
   - Цвет при наведении (Hover) и выбранном состоянии (Selected): `#b3f7ff` (циановый).
   - Цвет текста и линий: `#000000` (черный).
   - Кнопка имеет размеры примерно 300x80px (в исходном варианте, но в шапке кнопки пропорционально уменьшены для аккуратного внешнего вида).
2. **Анимация кнопки**:
   - Переход рамки из уголков и плюсика в сплошную линию реализуется за счет расширения псевдоэлементов/абсолютных блоков `.corner-tl`, `.corner-br` (до `100%` ширины и высоты) и втягивания/роста линий плюсика `.plus-line-v` и `.plus-line-h` при ховере или в классе `.selected`.
   - Длина усиков плюсика и уголков в исходном состоянии установлена в `20px` со всех сторон (для обеспечения визуальной симметрии).
   - Чтобы передняя серая панель кнопки `.btn-body` не перекрывала внутренние усики плюсика и уголков в исходном и ховер состояниях, фон переднего слоя в этих состояниях сделан прозрачным (`background-color: transparent`). Серый или циановый фон в этих состояниях рендерится исключительно на заднем слое `.btn-shadow`.
   - При клике передний слой кнопки `.btn-body` становится непрозрачным (циановым) и сдвигается по `transform: translate(12px, -12px)` (или меньшему значению в зависимости от размера кнопки), в то время как задний слой `.btn-shadow` с рамкой остается неподвижным и возвращает серый цвет `#ebebeb`.
3. **Анимация и сжатие шапки**:
   - Шапка (`.site-header`) имеет два состояния по высоте: сжатое (`60px` по умолчанию) и раскрытое (`100px` при `:hover`). Это экономит экранное пространство и предотвращает перекрытие основного контента, когда курсор находится вне верхней зоны.
   - Круг логотипа (`.logo-circle`) и текст («Лого») плавно масштабируются синхронно с высотой шапки за счет CSS-переходов.
4. **Адаптивность элементов шапки**:
   - Вместо скрытия элементов в бургер-меню (которое отложено), кнопки остаются в горизонтальном флекс-контейнере и пропорционально уменьшаются (до `100px` на ноутбуках и до `80px` на мобильных экранах в покое) при переходе на меньшие разрешения (медиа-запросы `< 1440px` и `< 768px`). Декоративные элементы кнопок (вылеты плюсиков, длина усиков уголков) переопределяются в CSS для сохранения точных визуальных пропорций.
5. **Многостраничность и навигация**:
   - Маршрутизация на бэкенде: Flask обрабатывает роуты `/` (Главная страница) и `/resume` (Резюме).
   - Навигационные элементы в шапке переопределены как теги `<a>` с прямыми ссылками на соответствующие разделы.
   - **Активность nav-кнопки управляется бэкендом**: `render_template(..., active='home'|'resume')` передаёт в контекст переменную `active`; partial шапки `_header.html` ставит класс `.selected` на нужную кнопку через параметр `active` макроса `retro_button` (раньше `selected` был захардкожен прямо в HTML каждого шаблона).
   - Разделение функционала: боковое меню «Содержание» отображается только на странице Резюме.
   - Плавная прокрутка: на странице Резюме клики на пункты бокового меню плавно прокручивают страницу к секциям резюме с помощью связки `data-target` атрибутов и JS-метода `scrollContainer.scrollTo()` с вычислением точных координат вьюпорта. Это решает баг мобильных Chromium-браузеров (Samsung Internet, Yandex Browser), в которых вызов `scrollIntoView()` зависал или блокировался из-за конфликта с `overflow: hidden` у родительского `body`.
6. **Шаблонизация Jinja2 (устранение дублирования HTML)**:
   - Вёрстка типовых элементов вынесена из `index.html`/`resume.html` в переиспользуемые файлы: `base.html` (наследование), `macros.html` (макрос `retro_button`), `_header.html` и `_sidebar_menu.html` (partial'ы через `{% include %}`).
   - **Макрос `retro_button(href, label, id, active, classes, aria)`**: инкапсулирует многослойную структуру ретро-кнопки (задний слой `.btn-shadow` с уголками и плюсиком + передний `.btn-body` с тремя текстовыми span'ами idle/hover/selected) в одном вызове. Классы собираются через `['retro-btn'] + classes.split() + (['selected'] if active)` и склеиваются `join(' ')` — это исключает trailing-space в `class` при пустых/неактивных параметрах.
   - **`_header.html`**: рендерит логотип и обе nav-кнопки (`Главная`/`Резюме`) через макрос; активность берёт из контекста `active`. Подключается в `base.html` через `{% include %}`, поэтому шапка едина для всех страниц.
   - **`_sidebar_menu.html`**: 5 пунктов меню генерируются циклом `{% for %}` из списка `(target, text, is_first)` — разница только `data-target` и текст. Класс `collapsed` и активный пункт «Шапка» проставлены по умолчанию. Include'ится в `resume.html` через блок `body_extra`.
   - **`base.html`**: общий скелет (`<!DOCTYPE>`, `<head>` со шрифтом Inter + `style.css`, шапка, `<script src=main.js defer>`). Блоки: `title` (заголовок страницы), `head` (доп. теги — напр. `marked.js` в resume), `main_class` (доп. класс `<main>` — напр. `resume-layout`), `content` (основной контент), `body_extra` (доп. элементы перед `</body>` — сайдбар).
   - **Сокращение**: `index.html` 95→27 строк, `resume.html` 395→~290 строк (контент секций сохранён 1:1, шапка/сайдбар/head-теги ушли в base/include).
   - CSS и JS не дублировались изначально (уже в одном `style.css`/`main.js`) — шаблонизация затронула только HTML.
   - `References/components/` не тронут — там устаревшие макеты кнопки/меню, источник истины — продакшен-шаблоны.
7. **Контейнеризация и CD-пайплайн**:
   - Приложение обёрнуто в Docker-контейнер с сервером Gunicorn.
   - Nginx настроен на терминацию HTTPS (SSL от Let's Encrypt), проксирование трафика на Gunicorn (`http://app:5000`) и редирект HTTP -> HTTPS (порт 80 -> 443).
   - Внедрена схема с Xray (VLESS+Reality) на порту 443, пробрасывающая не-VLESS трафик на локальный порт `127.0.0.1:4000`, где его принимает Nginx.
   - Деплой автоматизирован через GitHub Actions с локальным раннером на VPS, работающим под юзером `deployer`. Управление жизненным циклом контейнеров осуществляется через пользовательскую службу `systemd --user restart my-website.service` с автоматическим обновлением юнит-файла и очисткой старых образов.

## Текущий прогресс
- [x] Шаг 1: Реализована обычная ретро-кнопка (из папки `Normal`). Настроены все анимации (смыкание рамки из уголков и плюсика на ховере, сдвиг цианового слоя на клике с сохранением серой тени и рамки под ней). Кнопка перенесена в левый верхний угол.
- [x] Шаг 2: Реализовано боковое меню «Содержание» справа по центру экрана.
  - Бесшовная черная окантовка толщиной `1px` огибает меню и выступающий язычок как единую фигуру. Вдоль правого края (у границы экрана) черная рамка отсутствует.
  - Высота язычка свертывания увеличена с `60px` до `80px`, а встроенная стрелка-уголок увеличена с `12px` до `16px` с сохранением оптического баланса при анимациях.
  - Меню растянуто по вертикали для придания визуальной "разреженности": отступы внутри пунктов увеличены до `14px 20px`, вертикальные отступы меню увеличены до `40px`, а расстояние между пунктами (`gap`) увеличено с `12px` до `20px`.
  - Сворачивание: меню уезжает вправо на `translateX(250px)`. В открытом состоянии стрелка указывает вправо `>` (направление задвигания), в закрытом — влево `<` (направление выдвигания).
  - Пункты меню обновлены на: Шапка (активный по умолчанию), Обо мне, Что я умею, Проекты, Интересное.
  - Пункты меню: неактивный пункт имеет серый фон и черный маркер-квадратик `12x12px`. При клике он переключается в активный (`.active`): фон загорается синим `#0059ff`, текст становится белым, добавляется темно-серая пунктирная рамка `#464646`. Черный квадратик поворачивается на `45°` и «раздваивается» на светло-голубой ромбик `#8cfff9` (сдвиг вверх на 6px) и темно-зеленый ромбик `#357774` (сдвиг вниз на 6px), которые накладываются друг на друга.
- [x] Шаг 3: Реализована адаптивная шапка сайта (header) с логотипом слева и кнопками навигации («Главная» и «Резюме») справа.
  - Внедрено hover-раскрытие шапки: по умолчанию шапка "сжата" (`60px` по высоте), при наведении курсора плавно увеличивается по высоте до `100px`, круглый логотип пропорционально масштабируется с `46px` до `80px`, а текст «Лого» — с `12px` до `18px`.
  - Кнопки навигации размещены по центру высоты шапки и сменяют активность (класс `.selected`) при кликах. Кнопка «Главная» активна по умолчанию.
  - Кнопки навигации динамически уменьшаются с `160x44px` (при ховере шапки) до `130x36px` (в покое), сохраняя пропорции с высотой шапки.
  - Настроена плавная адаптивность (через медиа-запросы `@media`): на экранах `< 1440px` шапка сжимается до `50px` / `80px` (ховер), а кнопки — до `100x30px` / `120x36px`. На экранах `< 768px` шапка сжимается до `45px` / `65px`, а кнопки — до мобильного формата `80x26px` / `95x32px`.
- [x] Шаг 4: Реализована многостраничность, разделы контента и Scroll Snapping.
  - Кнопки навигации переведены в теги `<a>` с прямыми ссылками на `/` и `/resume`.
  - На Главной странице убрано боковое меню и размещена стилизованная ретро-табличка «В разработке» (`.retro-window`) с кнопкой-ссылкой на резюме. Тень циановая влево-вниз (`box-shadow: -6px 6px 0px #b3f7ff`). Стиль шапки и кнопок унифицирован с окнами резюме (см. Шаг 10).
  - На странице резюме создана структура полноэкранного Scroll Snapping контейнера с 5 секциями (Шапка/Hero, Обо мне, Что я умею, Проекты, Интересное). При скролле страница плавно прилипает к началу каждой секции, имитируя смену слайдов, а боковое меню обеспечивает плавный скролл к ним.
  - Сверстана первая карточка резюме (Hero) с ФИО, специализацией, фото-плейсхолдером (300x360px) и целями по предоставленному референсу. Первый пункт бокового меню («Шапка») перенаправлен на скролл к этой секции.
- [x] Шаг 5: Оптимизировано распределение пространства и увеличены отступы на странице резюме для широких мониторов.
  - Ширина `.hero-card` на больших экранах увеличена до `1300px` (до `950px` для ноутбуков), а зазор `gap` между фото и описанием — до `150px` (до `70px` на ноутбуках).
  - Расширены вертикальные отступы внутри блока информации (отступ имени: `45px`, специализации: `90px`, зазор описания: `60px`), что позволило занять больше полезной площади экрана на разрешениях вроде 2033x1031 без искусственного увеличения шрифтов и плейсхолдера.
- [x] Шаг 6: Реализована вторая карточка резюме "Обо мне" в стиле плоского окна Notepad (Windows 98).
  - Сверстан контейнер `.notepad-window` с темно-синим заголовком, кнопками управления (свернуть, развернуть, закрыть) и белой рабочей областью.
  - Дизайн уплощен (убраны классические 3D-рамки), добавлена легкая ретро-тень.
  - Текст набран моноширинным шрифтом (`Consolas`/`Courier New`), в конце текста реализована CSS-анимация мигающего курсора `|` (`@keyframes blink`).
  - Окно адаптировано под широкие мониторы (`max-width: 1200px`), средние экраны и мобильные устройства с сохранением читаемости моноширинного текста.
- [x] Шаг 7: Реализована третья карточка резюме "Что я умею" в виде абстрактного окна с аккордеонами (Windows-style).
  - Сверстана серая плашка окна `.skills-window` (`max-width: 1200px`) с циановой тенью `box-shadow: -16px 16px 0px #b3f7ff` (направление тени изменено на влево-вниз).
  - Внутри окна размещены три аккордеона с кнопками-квадратами `+`/`-`, которые плавно подсвечиваются циановым цветом при наведении курсора.
  - Навыки оформлены в виде списков с кастомным ретро-маркером в виде квадратика `■`, масштабируемым через `em`. Отступ контента `.accordion-content` выровнен по линии текста заголовка.
  - Добавлена JS-логика: при открытии секции аккордеона все остальные секции автоматически закрываются. Плавность раскрытия обеспечивается за счет расчета `scrollHeight`.
  - Добавлен перерасчет `max-height` при событии `resize` для предотвращения обрезания текста на экранах разной ширины, а также поддержка доступности клавиатуры (`Enter` / `Space`).
  - Интегрирован Intersection Observer для автоматического переключения активных пунктов бокового меню при прокрутке разделов резюме, с защитой от ложных срабатываний во время плавного скролла по клику.
- [x] Шаг 8: Реализована четвертая карточка резюме "Проекты" в стиле Проводника Windows 95 (File Explorer).
  - В проект скопированы ретро-иконки компьютера, жесткого диска и закрытой папки в каталог `static/icons/`.
  - В секцию `#projects` шаблона `resume.html` встроена структура окна `.explorer-window` с синей панелью заголовка, строкой меню и двухпанельной рабочей областью. Окно снабжено циановой тенью влево-вниз (`box-shadow: -16px 16px 0px #b3f7ff`).
  - Левая панель `.explorer-sidebar` содержит иерархическое дерево каталогов со стилизованными пунктирными линиями связей в CSS, иконками и кнопками развертывания `+`/`-`. Пунктирные линии реализованы через `::before`/`::after` для `li`, а у корня `Kamil-PC` линии скрыты.
  - Правая панель `.explorer-content` отображает README.md выбранного репозитория. В `<head>` подключена библиотека `marked.js` через CDN для рендеринга Markdown в HTML на фронтенде.
  - Внедрена JS-логика: кнопки `+`/`-` и клик по папкам-веткам переключают класс `.expanded` (показывая/скрывая дочерние узлы), а выбор проектов-листьев `AI_chat` или `my-website` выделяет их синим цветом с точечным бордером фокуса и рендерит соответствующий README.md. Проект `AI_chat` открыт и отрендерен по умолчанию при загрузке.
  - Стилизованы скроллбары проводника (левой и правой панелей) под ретро-дизайн Windows 95 с шахматным узором дорожки и объемным бегунком, а глобальный скроллбар прокрутки секций полностью скрыт для сохранения бесшовности.
  - Сверстана мобильная адаптивность: на экранах `< 768px` двухпанельный лейаут перестраивается в вертикальный стек (сверху папки с ограниченной высотой и прокруткой, снизу — README.md).
- [x] Шаг 9: Реализована пятая карточка резюме "Интересное" в стиле настроек экрана Windows 95/98 (Display Properties), и проведены финальные улучшения интерфейса.
  - В секцию `#interests` шаблона `resume.html` встроена разметка `.display-window` с сине-голубым заголовком настроек экрана, вкладками («Спорт», «Сети и ПК», «Музыка», «ML & DL») и кнопками «ОК», «Отмена», «Применить» внизу.
  - Отрисована на чистом CSS 3D-модель CRT-монитора с подставкой и основанием. На экран наложены ретро-эффекты: мерцание люминофора (`flicker`) и строчная развертка (`scanlines`).
  - При переключении вкладок экран CRT-монитора кратковременно гаснет (имитируя перезагрузку электронного луча) и рендерит неоново-зеленый ASCII-арт.
  - Добавлена кнопка питания **Power** на корпусе монитора: нажатие гасит экран (led-светодиод становится серым, экран полностью чернеет).
  - Интегрирована JS-логика для переключения вкладок стрелками клавиатуры, а клик на «ОК», «Отмена» или крестик закрытия плавно скроллит страницу к началу (Hero-секции).
  - На экранах `< 768px` блок монитора масштабируется, а разметка перестраивается в вертикальный стек.
  - **Доработки Блокнота**: кнопки меню блокнота переведены на английский (`File`, `Edit`, `Search`, `Help`) и переименованы в класс `.notepad-menu-item`, чтобы полностью исключить кликабельность и пересечение с логикой бокового меню; текстовая зона получила внутренний 3D-эффект (утопленная белая рамка).
  - **Доработки Проводника**: добавлена пасхалка `project-dnd` с иконкой поврежденной папки и выводом ошибки чтения файла; исправлен баг вертикальных пунктирных линий (теперь они пронизывают элементы по высоте `::before` и корректно соединяются, не вися в пустоте).
- [x] Шаг 10: Унифицирован визуальный стиль ретро-окон на всём сайте (Главная + все карточки резюме).
  - Единый шаблон шапки окна: фон `#2b3f94`, отступы `6px 8px`, внутренняя нижняя граница `2px solid #a0a0a0`, заголовок `16px / 700 / Inter / letter-spacing 0.5px`. Единый набор из трёх кнопок управления `.win-btn` (`_` `□` `✕`, 24×24, `gap: 4px`, ретро-эффект `inset box-shadow`).
  - **Окно \"Интересное\" (`.display-window`)**: приведено к общему стилю — фон шапки `#000080`→`#2b3f94`, отступы `3px 4px`→`6px 8px`, добавлена нижняя граница `#a0a0a0`, шрифт заголовка 13px/bold→16px/700; одна кнопка-крестик заменена на три стандартные `.win-btn` (с сохранением `id="displayClose"` для JS-скролла наверх); удалён `padding: 2px` контейнера и переопределение уменьшенных кнопок.
  - **Окно на Главной (`.retro-window`)**: приведено к общему стилю — фон `#ebebeb`→`#c0c0c0`, граница `2px`→`1px`, тень `rgba(0,0,0,0.15)`→сплошная `#b3f7ff`; шапка `#0059ff`→`#2b3f94`, отступы `6px 10px`→`6px 8px`, нижняя граница `#000`→`#a0a0a0`; одна disabled-кнопка `X` (18×18) заменена на три стандартные `.win-btn`.
- [x] Шаг 11: Доработана карточка «Интересное» — увеличен CRT-монитор (4:3), переработаны эффекты выключения и обновления экрана.
  - **Размеры монитора**: корпус `320×240`→`400×300` (соотношение 4:3); экран `.crt-screen-container` переведён с фиксированной `height: 150px` на `flex: 1 1 auto` (теперь ~354×232, заполняет корпус без пустоты); bezel `justify-content: space-between`→`flex-start` + `gap: 8px`. Подставка `60×25`→`70×30`, основание `160×10`→`180×12`. Шрифт ASCII `8.5px`→`9px`. Мобильные размеры `260×200`→`280×210` (4:3), экран `flex:1`, основание `130`→`150`.
  - **Лейаут правой панели**: `.display-tab-layout` `align-items: center`→`stretch` (info-block тянется под высоту монитора); `.display-fieldset` `height: 230px`→`flex: 1 1 auto` (заполняет info-block). Окно `.display-window` выросло с ~444px до ~511px (почти равно окну «Проекты» ~530px), пустое пространство справа от монитора устранено. Все 4 ASCII-арта влезают (спорт 194×179, сети 340×132 — самый широкий, с запасом 14px).
  - **Эффект выключения (с нуля)**: вместо мгновенной чёрной заливки — классическое схлопывание: контент (ascii/scanlines/flicker) схлопывается по `scaleY(1)→scaleY(0.01)` в яркую циановую линию (`crt-collapse`, 0.42s, линия достигается на ~0.23s), яркая линия вспыхивает и тает (`crt-line-out` через `::after`, **с задержкой 0.2s** чтобы проявляться только после схлопывания контента, длительность 0.22s, `fill: both` + `opacity:0` в базовом правиле для невидимости во время задержки), экран затемняется (`crt-blackout`, задержка 0.28s). Обратное включение: линия раскрывается `scaleY(1)→scaleY(140)` в полный экран (`crt-line-in`), фон `#000→#0c100a` (`crt-poweron-bg`), контент проявляется с пересветом (`crt-content-in`). Класс `power-on` снимается через 600ms; `updateDisplayTab(tab, true)` пропускает beam-sweep при включении, чтобы эффекты не конфликтовали.
  - **Эффект обновления при смене вкладки / «ступеньки» (с нуля)**: вместо жёсткой 3-шаговой вспышки `white→black→green` (`crt-refresh`) — луч-развёртка: яркая горизонтальная полоса пробегает сверху вниз (`crt-beam-sweep` через `::before`, 0.3s), контент кратко вспыхивает `brightness(1)→2.2` (`crt-beam-glow`). Таймаут снятия класса `150ms`→`300ms` под новую длительность.
- [x] Шаг 12: Мобильная адаптация и уточнение вертикальных размеров (по результатам проверки с телефона).
  - **(1) Боковое меню изначально свёрнуто (десктоп + мобайл)**: в `resume.html` к `#sidebarMenu` добавлен класс `collapsed` по умолчанию. Меню уезжает вправо на `translateX(250px)`, остаётся видимым только язычок-стрелка (клик разворачивает). Десктоп сохраняет центр-правое положение язычка; на мобайле язычок перенесён (см. п.3).
  - **(2) Ховеры отключены на мобайле (`@media max-width:768px`)**: все `.site-header:hover ...` приравнены к сжатым значениям (шапка всегда `45px`, логотип `32px`, nav-кнопки `80×26` и т.д.) — залипший после тапа `:hover` не меняет вид. Сброшены ховеры логотипа, язычка меню, пунктов меню, дерева (`tree-toggle`), аккордеона, ретро-кнопок. Десктоп ховеры сохранены.
  - **(3) Перенос бокового меню на мобайле**: `.sidebar-menu` (`top:auto; bottom:20px; transform:translateX(0)`) и `.menu-toggle` (`top:auto; bottom:0; height:56px; transform:none`) — язычок в правом нижнем углу, не перекрывает центральный контент.
  - **(4) Центрирование карточек**: `.resume-section` `height:100vh`→`min-height:100vh` + добавлен `padding-bottom:60px` (симметрично с `padding-top`) → карточка по центру по вертикали (была на ~30px ниже). `.main-content` `padding-top:100px`→`60px` (симметрично) — табличка на Главной теперь по центру (offset 0px). На мобайле `padding:50px/50px`.
  - **(5) Скролл-снап на мобайле отключён**: `.resume-layout { scroll-snap-type:none }` + `.resume-section { scroll-snap-stop:normal }` в мобильном блоке — высокие карточки можно свободно дочитывать, снап не перебрасывает дальше. Десктоп `mandatory` + `always` сохранены. `min-height:100vh` позволяет секциям расти под tall-контент без обрезки низа.
  - **(6) Z-index в Проводнике**: inset-bevel рамки панелей (`explorer-sidebar`/`explorer-content`) перенесён с `box-shadow` самого элемента на псевдоэлемент `::after` (`position:absolute; inset:0; pointer-events:none; z-index:2`), элементу добавлен `position:relative`. Раньше скроллящийся текст рисовался поверх inset-тени рамки (inset box-shadow всегда ниже контента); теперь bevel рисуется поверх контента, и текст уходит под рамку, а не поверх неё.
  - **Важный нюанс порядка правил**: мобильные переопределения для `.resume-layout`/`.resume-section`/`.tree-toggle:hover`/`.accordion-header:hover` вынесены в отдельный блок `@media (max-width:768px)` **в конец файла** — их глобальные определения идут после блока шапки, и более поздние глобальные правила иначе перекрывали бы мобильные. Мобильные же правила шапки/меню/ретро-кнопок оставлены в блоке шапки (их глобалы выше).
- [x] Шаг 13: Фикс «тень съезжает вверх при скролле» на мобильных — разделение тени и контентного контейнера.
  - **Причина**: на мобайле окна программ (проводник, блокнот, навыки, дисплей) по высоте превышают viewport → страница (`.resume-layout`) скроллит само окно вместе с циановой тенью → тень «съезжает вверх». Внутренний скролл панелей тень не двигал (проверено), проблема была именно в страничном скролле окна.
  - **Решение (паттерн «окно в viewport + внутренний скролл»)**: в `@media (max-width:768px)` каждому окну добавлены `max-height: calc(100vh - 140px)` + `overflow: hidden`, а контентной области — `flex: 1 1 auto; min-height: 0; overflow-y: auto`. Окно (с тенью) зафиксировано в пределах экрана, скроллится только внутренний контент — тень неподвижна. `min-height: 0` критично для flex-детей, иначе они не сжимаются под `max-height` и контент переполняет окно.
  - **Проводник (`.explorer-window`)**: `max-height` + `overflow:hidden`; `.explorer-body` `height:500px`→`flex:1 1 auto; height:auto; min-height:0`; `.explorer-sidebar`/`.explorer-content` + `min-height:0` (дерево и README скроллятся внутри). Проверено: окно 493px (capped), влезает в 513px доступных; внутренний скролл README не двигает окно (top 70→70).
  - **Блокнот (`.notepad-window`)**: `max-height` + `overflow:hidden`; `.notepad-content` + `flex:1 1 auto; min-height:0; overflow-y:auto` (текст скроллится внутри).
  - **Навыки (`.skills-window`)**: `max-height` + `overflow-y:auto` (окно без шапки/меню — аккордеоны скроллятся прямо в нём, рамка/тень неподвижны).
  - **Дисплей (`.display-window`)**: `max-height` + `overflow:hidden`; `.display-tab-body` + `flex:1 1 auto; min-height:0; overflow-y:auto` (монитор+fieldset скроллятся внутри). На 1280×633 окно было 537px (overflow), стало 493px (capped).
  - **Десктоп не затронут**: все правки внутри `@media (max-width:768px)`, проверено — на 1280px `max-height:none`, `overflow:visible`, `explorer-body` 460px row. `overflow:hidden` на окне не клиппит его собственную циановую тень (outer box-shadow не обрезается собственным overflow). Ретро-окно на Главной (298px) в viewport влезает — фикс не потребовался.
- [x] Шаг 14: Разделение тени и контентного контейнера в проводнике (десктоп + мобайл) — «тень съезжает при скролле».
  - **Причина**: `.explorer-content`/`.explorer-sidebar` были одновременно и панелью-обрамлением (border + bevel), и скролл-контейнером (`overflow-y:auto`) — при скролле контента рамка/тень панели визуально «съезжали». Bevel через `::after` (из Шага 12 п.6) фиксировал наложение текста на рамку, но сама панель оставалась скролл-контейнером.
  - **Решение (по схеме пользователя)**: панель = неподвижное обрамление, внутри — отдельный скролл-див. HTML: `.markdown-body` обёрнут в новый `<div class="explorer-scroll">`; дерево уже имело внутренний `<div class="explorer-tree">` — он стал скролл-контейнером.
  - **CSS**: `.explorer-content`/`.explorer-sidebar` → `overflow: hidden; padding: 0; display: flex; flex-direction: column` (рамка + bevel `::after` z-index:2 неподвижны). Новые скролл-контейнеры `.explorer-scroll`/`.explorer-tree` → `flex: 1 1 auto; min-height: 0; overflow-y: auto; padding: 30px`/`15px 10px` (padding перенесён сюда с панелей). Bevel `::after` остаётся на панели и не двигается; текст скроллится ПОД ним.
  - **Скроллбары**: Win95-стили `::-webkit-scrollbar` перенесены с `.explorer-content`/`.explorer-sidebar` на `.explorer-scroll`/`.explorer-tree` (теперь скроллят они). Иначе скроллбар пропадал (стили были на нескроллящих панелях).
  - **Responsive**: 1440 — `.explorer-scroll { padding: 20px }` (вместо `.explorer-content`); 768 — `.explorer-sidebar`/`.explorer-content` `overflow:hidden; padding:0`, а `.explorer-tree { padding:10px 5px }`/`.explorer-scroll { padding:15px }`.
  - **Проверено (десктоп)**: `panelFixedOnScroll: true` — `.explorer-content` (top/left/h/w) не меняется при `scrollTop` внутреннего `.explorer-scroll`; страница при внутреннем скролле не двигается; `panelOverflow:hidden`, `innerOverflow:auto`, bevel `::after` z-index:2 на месте. Мобайл (симуляция): `contentMoved:false`, внутренний скролл работает, дерево скроллится. JS-ошибок 0. Скроллбар 16px в headless не рендерится (overlay-артефакт) — на реальном браузере Win95-скроллбар отобразится.
- [x] Шаг 15: Текст наезжает на bevel при скролле дерева — перенос padding с скролл-контейнера на рамку + z-index изоляция.
  - **Причина (Шаг 14 был недостаточен)**: Шаг 14 разделил frame/scroll, но padding остался на скролл-контейнере (`.explorer-tree { padding:15px 10px }`, `.explorer-scroll { padding:30px }`). Скролл-контейнер клиппит по padding-box (= border edge), поэтому текст при скролле проходит через padding-область (0..P px) и виден в зоне 3..P px рядом с bevel — воспринимается как «наезд на границу-тень». У README `padding:30px` (большой отступ) — наезд далеко от bevel, незаметен. У дерева `15px 10px` — близко, заметен. Bevel `::after` z-index:2 действительно поверх контента (доказано hit-test перехватом при `pointer-events:auto`), но текст в padding-области ВИДЕН ниже bevel-полосы (3..P px) — это и есть «наезд».
  - **Решение (чистое разделение по просьбе пользователя)**: padding перенесён с скролл-контейнера на **рамку**. Теперь скролл-контейнер `padding:0` клиппит по content-edge рамки (P px от border) — текст физически не может попасть в bevel-зону (0..3 px). Между bevel и текстом — чистый белый отступ рамки (padding-area, показывает bg рамки).
  - **CSS (base)**: `.explorer-sidebar { padding: 15px 10px }` (было 0), `.explorer-content { padding: 30px }` (было 0) — padding на рамках. `.explorer-tree { padding: 0; position: relative; z-index: 1 }` (было padding 15px 10px), `.explorer-scroll { padding: 0; position: relative; z-index: 1 }` (было padding 30px) — скролл-контейнеры без padding, изолированы в собственный stacking context НИЖЕ bevel (`::after` z-index:2). Двойная защита: текст клиппит до bevel + bevel поверх SC контента.
  - **CSS (responsive)**: 1440 — `.explorer-content { padding: 20px }` + `.explorer-scroll { padding: 0 }` (было наоборот). 768 — `.explorer-sidebar { padding: 10px 5px }` + `.explorer-tree { padding: 0 }`, `.explorer-content { padding: 15px }` + `.explorer-scroll { padding: 0 }` (было наоборот).
  - **Проверено (десктоп, воспроизведение скролла)**: `bevelZoneClean: true` для обеих панелей — в bevel-зоне (0..6 px от border frame) при скролле (`tree.scrollTop=200`, `scroll.scrollTop=130`) hit-test находит ТОЛЬКО frame (`.explorer-sidebar`/`.explorer-content`), без tree/markdown контента. `treeTopOffsetFromSidebarBorder: 17` (border 2 + padding 15) — tree клиппит на content-edge. z-order: `sidebarBevel:2 > tree:1`, `contentBevel:2 > scroll:1`. `panelFixed: true`. Мобайл (симуляция): `sidebarFixed:true`, tree на content-edge (offset 12 = border 2 + padding 10), bevel-зона чиста. JS-ошибок 0.
  - **Визуально**: позиция текста не изменилась (тот же отступ от border), но теперь между bevel и текстом — чистая padding-область рамки вместо скроллящегося контента. При скролле текст исчезает ровно по content-edge, не наезжая на bevel.
- [x] Шаг 16: Динамическая загрузка README.md с GitHub + адресная строка со ссылкой на репозиторий (карточка «Проекты»).
  - **Было**: `readmeData` в `main.js` — захардкоженный markdown-текст для двух проектов (`AI_chat`, `my-website`) и пасхалка `project-dnd` с фейковой «ошибкой чтения файла 0x80070570» и иконкой повреждённой папки (`w95_66.ico`). README не грузился с GitHub вовсе.
  - **Стало**: `main.js:181` — объект `projectConfig` с тремя проектами; каждый содержит `rawUrl` (raw-URL README на `raw.githubusercontent.com`) и `repoUrl` (корневая ссылка репозитория `github.com/.../repo`, без `/blob/`). `renderReadme()` стал `async`: `fetch(config.rawUrl, { cache:'no-cache' })` → `response.text()` → `marked.parse()` → рендер в `#readmeRenderer`. Результаты кэшируются в `readmeCache` (повторное переключение не грузит повторно). Состояния: загрузка (`.readme-loading`), ошибка сети (`.readme-error` с кодом HTTP), пустой выбор.
  - **Почему raw, а не blob/github.com**: GitHub `blob`-страница отдаёт HTML-обёртку, не markdown. `raw.githubusercontent.com` отдаёт чистый `text/plain` с CORS `Access-Control-Allow-Origin: *` (проверено curl по всем трём репозиториям) — `fetch()` работает напрямую из браузера, серверный прокси не нужен.
  - **Ветка**: `AI_chat` и `my-website` — `main`; `project-dnd` — `master` (на `main` README отдаёт 404, проверено). Ветка захардкожена в `rawUrl` каждого проекта.
  - **Адресная строка**: новый блок `.explorer-addressbar` в HTML (`resume.html`) между меню-баром (File/Edit/View/Tools/Help) и рабочей областью `.explorer-body` — Win2000-style: подпись «Адрес» + inset-bevel поле (тёмные границы сверху/слева, светлые снизу/справа — утопленное) с синей ссылкой `#addressLink` (`#0000ee`, underline, `text-overflow:ellipsis` для длинных URL). `target="_blank" rel="noopener noreferrer"`. При переключении проекта `renderReadme` обновляет `href`/`textContent` ссылки на `config.repoUrl`.
  - **Иконка project-dnd**: `w95_66.ico` (повреждённая папка) заменена на `w2k_closed_folder.ico` (обычная закрытая папка) — старая пасхалка с фейковой ошибкой устарела, README теперь реально грузится. «Заброшенность» сообщает сам текст README с GitHub.
  - **Адаптив**: `@media (max-width:768px)` — адресная строка `padding:4px 8px`, шрифт подписи/ссылки `11px` (симметрично с `menubar-item: 12px`).
  - **Проверено (браузер, http://127.0.0.1:5000/resume)**: при загрузке README `AI_chat` авто-рендерится с GitHub (H1 «Проект "AI чат"»); клик по `my-website` → адрес `github.com/Kamil-K-prog/my-website`, контент «В этом репозитории…»; клик по `project-dnd` → адрес `github.com/Kamil-K-prog/project-dnd`, контент с H1 «ПРОЕКТ ЗАБРОШЕН…» (37 эл-тов, реальный README, не фейк). Геометрия: меню-бар(bottom 765) → адресная строка(top 765, bottom 804) → body(top 804) — стыки без наездов (`overlap: true,true`). Ссылка `color: rgb(0,0,238)`, `fits: true` (URL влезает без ellipsis). JS-ошибок 0.
  - **Нюанс (на стороне GitHub, не сайта)**: README `project-dnd` начинается со строки «ПРОЕКТ ЗАБРОШЕН» — в устной формулировке «временно заброшен». Расхождение правится только редактированием README в репозитории, сайт грузит «как есть».
- [x] Шаг 17: Замена плейсхолдера-логотипа на `images/logo.svg` (десктоп + мобайл, оба шаблона).
  - **Контекст**: пользователь добавил `static/images/logo.svg` (векторный SVG, canvas 1024×1024, ~1.3 КБ — круг с чёрной обводкой и стилизованной подписью) и перенёс `static/icons/` в `static/images/icons/` — все 8 ссылок в `resume.html` уже обновлены на `images/icons/...` пользователем, перенос не сломал ничего. Ранее логотип был JPEG (`logo.jpg`, 1024×1024, 335 КБ), позже заменён на векторный SVG.
  - **HTML**: `index.html:19-21` и `resume.html:21-23` — `<span class="logo-text">Лого</span>` заменён на `<img class="logo-img" src="images/logo.svg" alt="Логотип">` (структура плейсхолдера была одинаковой в обоих шаблонах; после Шага 18 путь рендерится через `{{ url_for('static', filename='images/logo.svg') }}` в `_header.html`).
  - **CSS (`.logo-circle`)**: добавлен `overflow: hidden` — квадратное img обрезается по кругу `border-radius: 50%` (без этого img рисуется квадратным поверх круга). Остальные свойства круга (border `1px solid #000`, flex-center, transition) не тронуты.
  - **CSS (`.logo-img`)**: новое правило `width: 100%; height: 100%; object-fit: cover; display: block` — логотип заполняет круг без искажения пропорций (`cover` вписывает квадрат 1024×1024 в круглый контейнер любого размера; для SVG `object-fit` не искажает вектор, лишь задаёт viewport). Масштабирование при hover/адаптиве НЕ требует отдельных override: img следует за `.logo-circle` через `100%`, какой бы размер ни задал брейкпоинт/hover (46→80 десктоп, 38→65 на 1440, 32 на 768).
  - **Удалено**: `.logo-text` (базовое + `:hover`) и мёртвые `font-size` override для `.logo-text` в `@media 1440` и `@media 768` — текстового плейсхолдера больше нет, эти правила были бы мёртвым кодом. Полная проверка: `grep logo-text` по проекту → 0 совпадений.
  - **Проверено (браузер, http://127.0.0.1:5000/ и /resume)**: img грузится (`complete: true`, `src: .../static/images/logo.svg`, HTTP 200; `naturalWidth: 1024` берётся из атрибута `width="1024"` SVG). Геометрия покоя (брейкпоинт 1440): circle 38×38 border-box, img 36×36 content-box = circle − 2px border — `imgAlways2pxLessThanCircle: true`, `ratioMaintained: true`. Клипинг: `border-radius: 50%` + `overflow: hidden` + `object-fit: cover`. CSS-каскад hover подтверждён напрямую: `.site-header:hover .logo-circle` → 65px (1440 media-block, активен), 80px (десктоп base). Логотип виден на обеих страницах (`image "Логотип"` в accessibility-tree). JS-ошибок 0.
- [x] Шаг 18: Шаблонизация Jinja2 — вынесение типовых элементов вёрстки (шапка, ретро-кнопка, боковое меню) в отдельные файлы для устранения дублирования HTML.
  - **Контекст**: шапка сайта целиком дублировалась в `index.html` и `resume.html`; многослойная структура ретро-кнопки (задний слой с уголками/плюсиком + передний слой с тремя текстовыми span'ами) копипастилась 5 раз (2 nav × 2 страницы + 1 inline на Главной); боковое меню (50 строк) лежало inline в `resume.html`. CSS (`style.css`) и JS (`main.js`) уже были в одном файле — дублировался только HTML.
  - **Создано 4 файла**: `templates/base.html` (базовый скелет, наследование `{% extends %}`), `templates/macros.html` (макрос `retro_button`), `templates/_header.html` (partial шапки), `templates/_sidebar_menu.html` (partial сайдбара). Подробности архитектуры — в техрешении №6 выше.
  - **`app.py`**: `render_template('index.html')` → `render_template('index.html', active='home')`, `resume` — `active='resume'`. Переменная `active` течёт в контекст → `_header.html` ставит `.selected` на нужную nav-кнопку (раньше класс был захардкожен в HTML).
  - **Макрос `retro_button`**: классы собираются списком `['retro-btn'] + classes.split() + (['selected'] if active else [])` и склеиваются `join(' ')` — устранён trailing-space в `class`, который давала первоначальная строковая интерполяция `{{ classes }} {{ 'selected' if active else '' }}` (при `active=false` оставался пробел перед закрывающей кавычкой).
  - **Сайдбар**: 5 пунктов генерируются циклом `{% for target, text, is_first in items %}` из списка; `collapsed` и активный «Шапка» — по умолчанию. Include'ится в `resume.html` через блок `body_extra` (на Главной сайдбара нет).
  - **Сокращение объёма**: `index.html` 95→27 строк (−71%), `resume.html` 395→~290 строк (−27%). Контент всех 5 секций резюме сохранён 1:1 — изменена только обвязка (шапка/head/сайдбар ушли в base/include).
  - **`__pycache__/` убран из git**: закоммиченный `app.cpython-314.pyc` (артефакт запущенного Flask) удалён из индекса через `git rm --cached`; в `.gitignore` добавлены `__pycache__/` и `*.pyc`.
  - **Проверено (браузер, http://127.0.0.1:5000/ и /resume)**: обе страницы HTTP 200, 0 JS-ошибок в консоли. Рендер nav-кнопок: на `/` → `btnHome` в `selected`, `btnResume` без; на `/resume` → `btnHome` без, `btnResume` в `selected`. Inline-кнопка на Главной — `class="retro-btn inline-btn"` (без `selected`, без trailing-space). Сайдбар: клик по язычку снимает `collapsed` (`true→false`), клик по «Обо мне» переключает active (`hero→about`). README AI_chat грузится с GitHub (H1 «Проект "AI чат"» в DOM). Структура кнопок в accessibility-tree: по 3 текстовых span (idle/hover/selected) в каждой nav-кнопке — макрос отрабатывает корректно. Trailing-space regression check: 0 на обеих страницах.
- [x] Шаг 19: Настройка CD-пайплайна и деплоя на VPS.
  - Написаны `Dockerfile`, `.dockerignore`, `nginx/nginx.conf` и `docker-compose.yml` (приложение Gunicorn + Nginx sidecar).
  - Настроен редирект HTTP -> HTTPS (порт 80 -> 443) на стороне Nginx.
  - Написан `.github/workflows/deploy.yml` для автоматического деплоя (джоба сборки на Github runner, джоба деплоя на self-hosted VPS).
  - Настроен пользовательский systemd-сервис `my-website.service` на VPS под пользователем `deployer`.
  - Успешно протестирован весь пайплайн от пуша в `main` до smoke-теста.


